# Frontend application configuration
# Copy to frontend/.env and fill in required values.

# --- Required ---

# Backend API base URL (must be reachable from the browser)
NEXT_PUBLIC_API_URL=http://localhost:8000/api
